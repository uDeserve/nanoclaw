# HealthClaw Handoff (2026-04-14)

## Purpose

This document is a practical handoff note for the next contributor working on
HealthClaw.

It is intended to answer:

1. What is the current source of truth?
2. What has already been verified?
3. What HealthClaw functionality is already implemented?
4. What should be done next?

## Source Of Truth

Primary implementation repository:

- server repo: `/data/lsj/healthclaw`

Supporting local archive:

- Windows repo: `E:\healthclaw\healthclaw`

Working rule:

- treat the server repo as the authoritative codebase
- use the Windows repo mainly for documentation archive and convenience reading
- after each meaningful milestone:
  - update docs under `docs/`
  - commit and push from the server repo to `origin`
  - sync important docs back to the Windows archive if needed

## Verified Environment

### Server

- host alias: `lsj245`
- ssh target: `lsj@10.82.1.245:1181`
- project root: `/data/lsj/healthclaw`
- conda env: `/data/lsj/conda-envs/healthclaw`
- Miniconda root: `/opt/miniconda3`

Correct activation sequence:

```bash
source /opt/miniconda3/etc/profile.d/conda.sh
conda activate /data/lsj/conda-envs/healthclaw
```

Verified toolchain in the activated environment on 2026-04-14:

- `node v22.22.2`
- `npm 10.9.7`

Important environment note:

- non-activated SSH shells may fall back to system `node v12.22.9`
- if that happens, `npm` may fail with `Cannot find module 'node:path'`
- always activate the conda environment before running build/test commands

### Local Windows Archive

- repo path: `E:\healthclaw\healthclaw`
- intended role: documentation archive and fallback reference

## Current Repository Reality

The Windows archive and the server repo are not equivalent.

Current observed state:

- the Windows repo contains the HealthClaw documents, but its tracked source
  tree is still largely upstream NanoClaw
- the server repo contains the real HealthClaw implementation under
  `src/healthclaw/`

This means all implementation reading, coding, testing, and release work should
be done against `/data/lsj/healthclaw`.

## Verified Server Status

Observed on the server on 2026-04-14:

- current branch: `main`
- recent HealthClaw commits exist on top of NanoClaw base
- worktree was effectively clean except for untracked `nanoclaw.pid`

Recent relevant commits:

- `55ae6cb Add symptom follow-up planner`
- `eb99ae9 Add multi-turn symptom follow-up merging`
- `40e91eb Add HealthClaw handoff documentation`
- `7153713 Add HealthClaw auto routing gate`
- `040e91eb Strengthen symptom triage structuring`
- `f457a06 Add initial HealthClaw skeleton runtime`

## Build And Test Verification

The server repo was re-verified on 2026-04-14 inside the conda environment.

Passed:

- `npm run build`
- `npm test`

Observed result:

- `24` test files passed
- `272` tests passed

This confirms that the current checked-out server code is in a healthy state at
handoff time.

## Implemented HealthClaw Functionality

The current implementation is a conservative first-stage HealthClaw runtime
layer on top of NanoClaw.

Implemented and verified in code:

- HealthClaw feature gate in `src/config.ts`
- host runtime integration in `src/index.ts`
- explicit `/healthclaw ...` command entry
- optional automatic routing of medically relevant messages
- task-template classification skeleton
- deterministic symptom fact extraction
- deterministic symptom safety precheck
- deterministic medication fact extraction
- deterministic medication safety precheck
- local structured medication reference support
- shared `medical_case_state` abstraction
- patient-facing response formatting
- expert-facing structured output
- SQLite persistence for medical traces and trace events
- multi-turn symptom follow-up merging using `parentTraceId`
- deterministic follow-up planner for missing or still-important fields
- first host-side `medication_consult` path

Main implementation files:

- `src/healthclaw/types.ts`
- `src/healthclaw/templates/registry.ts`
- `src/healthclaw/triage/symptom.ts`
- `src/healthclaw/medication/consult.ts`
- `src/healthclaw/medication/reference.ts`
- `src/healthclaw/runtime/case-state.ts`
- `src/healthclaw/runtime/command.ts`
- `src/healthclaw/runtime/routing.ts`
- `src/healthclaw/runtime/handle-medical-message.ts`
- `src/db.ts`
- `src/index.ts`

## Current Architectural Reality

The code currently implements the first safe host-side medical path.

Important interpretation:

- HealthClaw does not yet run as a fully developed container-side medical
  specialist-agent system
- the current strongest path is a host-side deterministic `symptom_triage`
  pipeline
- for HealthClaw-routed messages, the host currently handles classification,
  fact extraction, safety assessment, follow-up planning, patient view,
  expert view, and trace persistence directly
- the generic NanoClaw container agent path remains intact for non-HealthClaw
  traffic

This is consistent with the staged strategy in the project documents:

- build a safe and traceable host-side medical runtime first
- expand to richer specialist-agent orchestration later

The runtime now also has an explicit shared case-state layer, which is the
current bridge between per-template extraction and future planner-oriented
multi-turn behavior.

That bridge is now active for both strongest host-side task paths:

- symptom follow-up continuity
- medication follow-up continuity

## Current Strongest Path

The most complete path today is:

- `symptom_triage`
- `medication_consult` (host-side version with local reference hardening)
- `report_interpretation` (first safe host-side text-report path)

What it already does well:

- identifies symptom-like medical messages
- extracts structured symptom fields such as duration, severity, age,
  temperature, location, onset, and associated symptoms
- checks deterministic red-flag rules
- decides a conservative disposition
- asks follow-up questions when required details are missing
- continues a recent incomplete symptom case across turns
- continues a recent incomplete medication consult across turns
- interprets pasted report text through a deterministic first report path
- persists a linked medical trace for audit/debugging

## Current Gaps

The following are not complete yet:

- broader `medication_consult` reference coverage
- broader `report_interpretation` finding coverage
- report multi-turn continuity
- deep `imaging_qa` logic
- external medical knowledge provider integration
- structured evidence retrieval beyond current rule and fact traces
- explicit patient profile memory flow
- container-side main-agent plus specialist-agent medical orchestration
- richer case-level memory stitching beyond current symptom and medication
  follow-up trace link

## Document Set To Trust

When resuming work, read these first:

1. `README_healthclaw.md`
2. `docs/SETUP_AND_STATUS_2026-04-13.md`
3. `docs/HEALTHCLAW_RUNTIME_PLAN.md`
4. `docs/HEALTHCLAW_KNOWLEDGE_STRATEGY.md`

For product-level intent, also read:

- `E:\healthclaw\docs\HEALTHCLAW_CONCEPT.md`
- `E:\healthclaw\docs\README_FOR_AGENT.md`
- `E:\healthclaw\docs\ARCHITECTURE_DECISIONS.md`
- `E:\healthclaw\docs\TRACE_SCHEMA.md`
- `E:\healthclaw\docs\TASK_TEMPLATES_V1.md`

## Recommended Next Step

Recommended next engineering milestone:

- deepen `report_interpretation` conservatively before starting imaging
  orchestration groundwork

Practical scope for that next milestone:

- expand deterministic report abnormality coverage in a controlled way
- consider whether report follow-up continuity should reuse the existing
  case-state pattern
- keep report input text-only for now
- avoid jumping into multimodal imaging execution in the next change

Practical recommendation:

- do not expand platform breadth first
- keep working inside the HealthClaw application layer
- preserve NanoClaw default behavior when HealthClaw is disabled
- keep safety and traceability visible in every change

## Working Rules For The Next Contributor

1. Activate the conda environment before any build or test command.
2. Treat `/data/lsj/healthclaw` as the code source of truth.
3. Keep HealthClaw changes isolated and conservative.
4. Do not break the default NanoClaw runtime path.
5. After each meaningful milestone:
   - update `docs/`
   - run build and tests in the conda environment
   - commit and push the server repo to GitHub
   - sync important docs back to the Windows archive if needed
